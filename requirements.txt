# FastAPI con dependencias estándar (incluye uvicorn, pydantic, etc.)
fastapi[standard]==0.115.0

# Servidor ASGI de alto rendimiento (ya incluido en fastapi[standard] pero especificado para claridad)
uvicorn[standard]==0.32.0

# Validación de datos (ya incluido en fastapi[standard])
pydantic==2.9.2

# Para manejo de formularios multipart (ya incluido en fastapi[standard])
python-multipart==0.0.12

# Opcional: Para mejor rendimiento JSON
orjson==3.10.7
